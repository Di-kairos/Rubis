import EscapementCore

/// Database, scanner, metadata and cover cache (SPEC §5).
/// Implemented in Phases 2 and 4. GRDB is added in Phase 2.
public enum MusicLibraryInfo {
    public static let logSubsystem = Log.subsystem
}
