import EscapementCore

/// Audio output, device control and playback queue (SPEC §4).
/// Implemented in Phase 3. SFBAudioEngine and CAAudioHardware are added there.
public enum PlaybackEngineInfo {
    public static let logSubsystem = Log.subsystem
}
