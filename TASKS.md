# TASKS.md — план работ

Восемь фаз. Каждая заканчивается работающим состоянием, которое можно запустить и проверить.
Фаза не закрыта, пока не выполнены **все** acceptance criteria.

Отмечай выполненное прямо в этом файле.

---

## Фаза 0 — Каркас

**Цель:** пустое окно, которое собирается и запускается.

- [x] Xcode-проект `Escapement`, target macOS 15.0, arm64, Swift 6 strict concurrency
      _(генерируется XcodeGen из `project.yml` — правки проекта делать там, затем `xcodegen generate`)_
- [x] `Config/Escapement.xcconfig` со всеми идентификаторами (SPEC §14)
- [x] Пять локальных SPM-пакетов из SPEC §3.1, пустые, но с тестовыми таргетами
- [x] Тест, проверяющий правило зависимостей между пакетами (парсит `Package.swift`)
- [x] `swift-format` конфиг + скрипт `Tools/format.sh` (+ `Tools/test.sh` для CLT)
- [x] `.gitignore`, `README.md` с командами сборки
- [x] Entitlements: hardened runtime вкл. (xcconfig), sandbox выкл. — проверено сборкой
- [x] `OSLog` категории `audio` / `library` / `network` / `ui`

**Acceptance:** `xcodebuild -scheme Escapement` без warnings; `swift test` зелёный;
приложение запускается и показывает пустое окно 900×600 с hidden titlebar.

---

## Фаза 1 — Дизайн-система

**Цель:** все токены и примитивы существуют и видны в превью до того, как появилась логика.

- [x] `DS.Color` — оба режима из DESIGN §2, адаптивные через dynamic provider
- [x] `DS.Font`, `DS.Space`, `DS.Radius`, `DS.Motion` (+ `DS.Metrics`, `DS.Shadow`)
- [x] Проверка контраста в тесте (расчёт WCAG-коэффициента для пар токенов)
- [x] Примитивы: `DSText`, `DSIconButton`, `DSListRow`, `DSSectionHeader`, `DSSearchField`,
      `DSProgressBar`, `DSCoverImage` (с плейсхолдером), `DSTechBadge`
- [x] `#Preview` для каждого — light + dark; неактивное окно через `controlActiveState`
      (accent → accentMuted в `DSProgressBar`/`DSTechBadge`)
- [x] Демо-сцена `DesignSystemGallery` (только DEBUG), открывается по `⌘⇧D`
      _(⌘⌥D из ТЗ занят системным «скрыть Dock» — перехватывается до приложения)_

**Acceptance:** галерея открывается, все компоненты корректны в обоих режимах,
тест контраста проходит, ни одного литерала цвета/размера вне пакета.

---

## Фаза 2 — База данных и модель

**Цель:** схема БД, миграции, домен.

- [x] Модели в `EscapementCore`: `Track`, `Album`, `Artist`, `Playlist`, `Source`, `PlayHistoryEntry` — все `Sendable`
- [x] GRDB `DatabasePool`, WAL, путь по SPEC §5.1
- [x] Миграции: `v1_initial` со всеми таблицами, индексами и FTS5
      _(FTS5: добавлен `contentless_delete=1` — contentless-таблица без него не умеет
      удалять строки из триггеров; семантика схемы SPEC сохранена)_
- [x] Триггеры синхронизации `track` → `track_fts`
- [x] Репозитории: `TrackRepository`, `AlbumRepository` (+`ArtistRepository`), `PlaylistRepository`, `SourceRepository`
- [x] `ValueObservation`-обёртки, отдающие async-последовательности на MainActor
- [x] Утилиты нормализации: `sortName(for:)`, casefold, снятие диакритики, «The » отбрасывается
- [x] Тесты: миграции с нуля, CRUD, FTS-поиск с диакритикой и кириллицей

**Acceptance:** тест вставляет 100 000 сгенерированных треков, поиск по FTS5
укладывается в 50 мс, наблюдение отдаёт обновления.

---

## Фаза 3 — Аудио-движок (ключевая фаза)

**Цель:** bit-perfect воспроизведение локального файла. Без UI.

- [x] Интеграция `SFBAudioEngine` и `CAAudioHardware`
- [x] `AudioDeviceController`: перечисление устройств, чтение доступных частот,
      установка nominal sample rate, hog mode, `SupportsMixing` (raw HAL — обёртки в CAAudioHardware нет),
      наблюдение за отключением
- [x] Логика выбора частоты из SPEC §4.2.3, включая политику при отсутствии точной
- [x] `Player` (actor): load / play / pause / seek / next / previous, очередь, gapless
- [x] `OutputStatus` и честный расчёт `isBitPerfect` (SPEC §4.5)
- [x] Пауза при смене частоты (§4.2.4) — только когда частота реально меняется
- [x] DSD: DoP при поддержке устройства (dsdRate/16 в available rates), иначе DSDPCMDecoder с пометкой
- [x] Управление громкостью через HAL, `deviceHasVolumeControl` для скрытия слайдера (§4.4)
- [x] Обработка потери устройства: пауза, статус, без автовозобновления
- [x] `Tools/make-fixtures.sh` — 26 фикстур (sine+noise, 6 частот × 16/24 бит; DSD64 не генерится —
      у ffmpeg нет DSF-muxer, DSD проверяется вручную на живых файлах)
- [x] `Tools/audio-verify` — CLI побитового сравнения через loopback (BlackHole 2ch)
- [x] Тесты логики выбора частоты (без железа)

**Acceptance:**
1. ✅ `audio-verify` — побитовое совпадение 24/24 (44.1/48/88.2/96/176.4/192 кГц × 16/24 бит,
   sine + noise, BlackHole loopback, 2026-08-06). Верификация со вторым «тёплым» проходом:
   холодный старт графа после смены частоты глотает ~100 мс — см. риск ниже.
2. ⏳ Audio MIDI Setup — ручная проверка при первом живом прослушивании (фаза 5+).
3. ⏳ Gapless на живом альбоме — ручная проверка, нужны реальные файлы.
4. ✅ `isBitPerfect == false` при деградации — verify гонял shared-режим (hog off),
   формула `exclusive && exact` даёт false; бейдж не врёт в плюс.

> **Риск (из verify):** после смены частоты устройства холодный аудио-граф теряет
> первые ~100 мс воспроизведения. В verify обойдено прогревочным проходом. В проде
> проверить на живом DAC первый трек после смены частоты; при подтверждении — preroll
> тишиной в `PlaybackEngine` перед первым треком новой частоты.

> Не переходить к фазе 4, пока пункт 1 не выполнен. Это фундамент всего продукта.

---

## Фаза 4 — Локальная библиотека

**Цель:** сканирование папки в БД.

- [x] `LibraryScanner`: обход, security-scoped bookmarks, инкрементальность по mtime+size
- [x] Чтение метаданных через `AudioFile`, маппинг по правилам SPEC §5.3
      _(MusicLibrary получил зависимость SFBAudioEngine — та же из тройки §3.2, ради AudioFile)_
- [x] Определение альбомов и компиляций, разрешение album artist
- [x] Извлечение и кэширование обложек (embedded), три размера, ленивая генерация (SPEC §5.4);
      folder-cover fallback (`cover.jpg` рядом) — при подключении UI в фазе 5
- [x] Параллельное чтение через `TaskGroup`, батчевая запись транзакциями по 500
- [x] `AsyncThrowingStream<ScanProgress>`
- [x] FSEvents-наблюдение с дебаунсом 2 с (`FolderWatcher`, MainActor)
- [x] Обработка битых файлов: scan.log, список failed в ScanSummary, скан не падает
- [x] Perf-тест скана на 10 000 файлов (APFS-клоны; факт: cold+warm 5.2 с при бюджете 48 с/3 с)

**Acceptance:** скан 50 000 треков ≤ 4 минут, повторный ≤ 15 секунд,
изменение файла на диске подхватывается автоматически,
битый файл не роняет процесс.

---

## Фаза 5 — Интерфейс

**Цель:** приложение, которым можно пользоваться каждый день.

- [ ] Главное окно: сайдбар / список / детали + транспортная панель
- [ ] Разделы: Now Playing, Albums, Artists, Tracks, Recently Added, Playlists
- [ ] Сетка альбомов и экран альбома (DESIGN §5.3, §5.4)
- [ ] Список треков с сортировкой, мультивыделением, индикатором играющего
- [ ] Транспортная панель целиком, включая технический badge и его поповер (SPEC §7.3)
- [ ] Поиск `⌘F` с группировкой результатов и клавиатурной навигацией
- [ ] Плейлисты: создание, drag & drop, переупорядочивание, удаление
- [ ] Все горячие клавиши из SPEC §7.6
- [ ] Настройки: вкладки Library, Audio, Server (заглушка до фазы 6), Keys
- [ ] Пустые состояния и inline-ошибки (SPEC §9)
- [ ] Проверка производительности скролла на 100k треков; переход на `NSTableView`,
      если SwiftUI не укладывается в бюджет

**Acceptance:** бюджеты SPEC §12 по старту, скроллу и поиску измерены и выполнены;
ни одного модального алерта вне двух разрешённых случаев;
навигация полностью работает с клавиатуры.

---

## Фаза 6 — Subsonic / Navidrome

**Цель:** второй источник, неотличимый в UI от локального.

- [ ] `SubsonicClient`: аутентификация salt+token, все эндпоинты из SPEC §6.1
- [ ] Хранение пароля в Keychain, Test connection с явным результатом
- [ ] Синхронизация каталога в те же таблицы, `source.kind = 'subsonic'`
- [ ] Загрузка обложек с сервера в общий кэш
- [ ] Download-then-play с префетчем следующего трека (SPEC §6.2)
- [ ] LRU-кэш потоков с лимитом и ручной очисткой
- [ ] Оффлайн-поведение: приглушение, исключение из shuffle, статус в сайдбаре
- [ ] Тесты парсинга ответов на зафиксированных JSON-фикстурах

**Acceptance:** треки с сервера играют bit-perfect (проверено badge и Audio MIDI Setup);
обрыв сети во время игры не роняет воспроизведение текущего трека;
пароль отсутствует в UserDefaults, БД и логах.

---

## Фаза 7 — Системная интеграция и шлифовка

- [ ] `MPNowPlayingInfoCenter` с обложкой, `MPRemoteCommandCenter`, медиа-клавиши
- [ ] Mini player (`⌘⌥M`), опциональный always-on-top
- [ ] Опциональная иконка в меню-баре
- [ ] Глобальные горячие клавиши с запросом Accessibility только при включении
- [ ] Восстановление состояния при запуске: очередь, позиция в треке, выбранный раздел
- [ ] Reduce Motion, Increase Contrast, accessibility labels
- [ ] `docs/manual-checklist.md` и полный прогон по нему

**Acceptance:** ручной чек-лист пройден целиком; 8 часов непрерывного
воспроизведения без единого dropout; память в покое ≤ 250 МБ.

---

## Что дальше (не делать сейчас)

Записано, чтобы не изобреталось по ходу: ReplayGain, Last.fm, smart-плейлисты,
запись тегов, статистика прослушиваний, sleep timer.
Каждый пункт — отдельное решение владельца, не инициатива агента.
