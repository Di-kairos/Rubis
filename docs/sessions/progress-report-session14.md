# Progress report — Session 14 (2026-09-14, Mac Mini)

Короткая сессия без правок кода. `main` остаётся `765bef9` (merge
`phase/10-audit`), docs `9c7c6a0`+.

## Сделано

- **Живое автообновление Sparkle подтверждено** (чек-лист §6.3). На этой
  машине стояла 0.10.0 (28); Sparkle в тихом режиме (`SUAutomaticallyUpdate = 1`,
  владелец когда-то выбрал «Automatically download and install») скачал
  `RubisMusic-0.10.2.dmg`, распаковал в кэш и держал `Autoupdate` + `Updater.app`
  до выхода из приложения. После Cmd+Q поставилась **0.10.2 (30)** — About
  подтверждает. Диалога в этом режиме нет по замыслу Sparkle, это не дефект.
- Заодно видно, что обложки из папок сканов (фича 0.10.2) на библиотеке
  владельца работают.

## В процессе

- Ничего: кодовых веток нет, дерево чистое.

## Осталось (переносится в kickoff 15)

1. **F03** — решение владельца по миграции `track.content_hash` (FLAC — MD5
   из STREAMINFO; прочие — SHA-256 первых 64 КиБ + размер; слияние только при
   совпадении). Вопрос задан, ответа пока нет.
2. Живые проверки `docs/manual-checklist.md` §9 (ЦАП/DoP с галкой на UID,
   gapless против Play Next, Space в Settings, Stop→Play→USB, битая БД).
3. Релиз **0.11.0** (`RUBIS_RELEASE=1 ./Tools/make-dmg.sh`) — ретест
   обновления остался только для перехода 0.10.2 → 0.11.0; механизм с 0.10.0
   доказан сегодня. §9.11 остаётся открытым (Sparkle 2.9.6 — в 0.11.0).
4. Остаток бэклога аудита — `AUDIT_RESPONSE_2026-09-12.md` §4.

## Решения

- Новых D-XXX нет. Тихий режим Sparkle оставлен как есть: поведение штатное,
  диалог в нём не показывается до выхода из приложения.
