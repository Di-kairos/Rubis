# Progress report — Session 16 (2026-09-17, Mac Mini)

Код `ef7bc62`, docs `408856f`+. Пакеты **284/284** (DesignSystem 9, Core 75,
Playback 53, Subsonic 44, MusicLibrary 103), `EscapementTests` **5/5**, Debug и
Release без warnings (Xcode 27). Версия поднята до **0.11.0 (31)**, не
опубликована.

## Сделано

- **Релиз 0.11.0 подготовлен, но не выпущен.** `861e1c3` — версия 0.11.0 (31).
  На Mac Mini нет Developer ID (0 identities), ключа Sparkle EdDSA и профиля
  `notarytool rubis` — `make-dmg.sh` в строгом режиме честно блокирует.
  Шаги и черновик заметок — `HANDOFF.md` «Релиз 0.11.0».
- **`Tools/test.sh` на Xcode 27** (`ec9cacd`): флаги Testing.framework из CLT
  подмешивались всегда и ломали `@Test` (`Testing.__SourceBounds`) — релизный
  путь падал бы на первом шаге и на MacBook. Теперь только при активном CLT.
- **Бэклог S15:** нечитаемая папка источника — строка в сайдбаре
  (`0c7053c`, `SourceStatusTests`); уборка двойников сравнивает mtime до
  секунды, как переезд (`687c603`, тест падал до правки).
- **Владелец делегировал UX-решения (D-020)** — решаю по конвенциям Music.app:
  - `69b9db7` — «Add to Playlist» в контекстном меню трека (+ New Playlist);
  - `b8f4ab7` — живой History (#30);
  - `fbe22e4` — shuffle/repeat и перемешанный порядок очереди между запусками
    (#16): снимок хранит исходный порядок + перестановку, `Player.restore`
    принимает порядок и режимы;
  - `0c3262c` — главная кнопка: Cancel во время загрузки, Retry после ошибки
    (#28); чинит залипающее «Loading…» после паузы;
  - `a0fea30` — выбранный недоступный трек не подменяется первым доступным;
  - `ef7bc62` — ←/→ листают полку альбомов, пока она в фокусе;
  - #19 (порт в ключе пароля) — осознанно отложен.
- Радио (radio-browser.info) обсуждено и **отклонено владельцем** — не
  предлагать.

## В процессе

- Ничего не висит незакоммиченным.

## Осталось

1. **Релиз 0.11.0 с MacBook** — `HANDOFF.md`, ретест 0.10.2 → 0.11.0 (§9.11).
2. **`audio-verify` 0/24** из-за TCC: у Terminal нет доступа к микрофону
   (`mic permission status: 2`). `Player.swift` менялся (`fbe22e4`, только
   раскладка очереди) — выдать доступ и перепрогнать до релиза, ждём 24/24.
3. **Живьём не проверено** всё из D-020: Add to Playlist, режимы после
   перезапуска, Cancel/Retry на серверном треке, стрелки на полке, строка
   нечитаемой папки. Пройти на стенде `build/run-live-check.sh`.
4. Живые проверки §9 (9.8, 9.5, 9.1, 9.2, 9.12; с ЦАПом 9.3/9.4/9.6), #26
   VoiceOver, #32 измерения — только с владельцем/железом.
5. Ждём перепроверки аудитора.

## Решения

- D-020 — продуктовые развилки решает Claude по конвенциям macOS (делегировано
  владельцем); записи по каждой развилке внутри.

## Грабли машины

- `audio-verify` из терминала без TCC микрофона даёт 0/24 «reference pattern
  not found» — смотреть строку `mic permission status` (нужно 3).
