# Progress report — session 13 (2026-09-12 … 14)

Машина: Mac Mini (CLT активен — все `swift test`/`xcodebuild` через
`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`). `main` на входе —
`f9017e3` (код `25cf06e`, 0.10.2), тесты 201/199/2. На выходе — `main`
**`765bef9`** (merge `phase/10-audit`, `--no-ff`), тесты **272/269/3** в пяти
пакетах + новый бандл приложения **EscapementTests 2/2**, Debug и Release без
warnings. Релиза не было.

## Что сделано

### Внешний аудит плеера — 33 пункта, три раунда

`AUDIT_PLAYER_2026-09-12.md` (6 P1, 25 P2, 2 P3). Перепроверил каждое
утверждение по коду до начала работ: ложных срабатываний ноль, три места аудит
недооценил (#17 — синк сервера hard-DELETE'ил треки с каскадом в плейлисты, это
P1; #22 — в `SubsonicSync` не было ни одной записи в журнал соединений; codec из
ответа сервера уходил в `appendingPathComponent` — запись вне каталога кэша).
Единая первопричина 01/08/17 — hard DELETE строк `track` при `ON DELETE CASCADE`
у `playlist_item`; закрыта одним правилом (D-018).

Шесть pack'ов, 22 кодовых коммита:

1. **Потеря данных и падения** — `retire()` переносит ссылки плейлистов до
   удаления строки; CUE-парсер отвергает `inf`/`nan`/переполнение/`00:99:99`
   (это был трап `Int64(Double)`); `append` после дыр в позициях; переходы
   «файл ↔ сегменты» с сохранением id; пропавшее у сервера гасится, а не
   удаляется; codec в имени кэша — только `[A-Za-z0-9]{1,8}`.
2. **Достоверный транспорт** — поколение команды с проверкой после каждого
   `await`; идемпотентный gapless; события движка одним упорядоченным потоком,
   `endOfAudio` только вслед за `renderingComplete` текущего; awaited teardown
   устройства; shuffle по вхождениям (`sourceIndices`); префетч вне цикла
   состояний; снимок очереди на каждой мутации.
3. **Честный тракт** — `OutputStatus` строится из открытого декодера (файл
   открывается ДО настройки устройства): разрядность `nil`, а не 16; микшер —
   результат `disableMixing`; DSD — применённый путь; политика частоты — снятая
   при настройке; стык gapless подменяет источник; receipt `Schema 2`, старые
   отчёты проверяются как прежде. Часы CUE-сегмента с нуля (обёртка декодера).
   **DoP — только по подтверждению владельца на UID + один физический формат
   с частотой, ≥24 бит и стерео (D-014).**
4. **Сервер и приватность** — кэш по серверу+аккаунту; проба скачанного тем же
   декодером по содержимому (закрывает и #14); Keychain `Update`→`Add`; ключ
   подписи receipt не ротируется при заблокированной связке; URL только
   http/https без userinfo; `Log.describe` без failing URL с токеном; журнал
   Network честен о границах (тексты аудитора); Sparkle 2.9.6.
5. **Управление и обратная связь** — `text.muted` ≥4.5:1 (D-015); Space/стрелки
   уходят любому текстовому полю по first responder; `FolderWatcher` наконец
   подключён; экраны перечитываются по `libraryRevision`; алерт вместо
   `fatalError`; Undo плейлистам / подтверждение источникам (D-016); история
   считает секунды дальше порога (D-017); ключ заметок по фактическому провайдеру.
6. **Поставка** — `RUBIS_RELEASE=1` строгий режим `make-dmg.sh`.

### Перепроверки аудитора — §13 и §14

§13 (R01–R07) — ремедиацию сделала параллельная сессия на другой машине
(`ea6e6d7`…`6716446`); здесь она проверена диффом по критериям приёмки и
принята, хвосты §13.3 закрыты (`50c51e4`): guard поколения в `stop()`,
применённая частота из HAL, DSD-проба читает блок.

§14 (F01–F04): **F01** — аудитор прав, `nowPlaying` не доказательство, а
`DecoderState` при dequeue декодер не трогает (`AudioPlayer.mm:294`), у движка
нет состояния «B ещё в очереди». Доказательство перенесено в сам декодер:
`ArmedDecoder` с `decode` под замком и `revoke()` — после отзыва ноль кадров,
`revoke()` говорит, ушли ли кадры до него, и только тогда сброс pipeline.
**F02** — CUE по кадрам 1/75 с + PERFORMER. **F04** — token транспорта
синхронно в обработчике до `Task`. **R07** — тестовый бандл приложения
`EscapementTests` (по совету аудитора, без переноса в пакет), `AlbumInfoService`
с инжектируемыми транспортом/разрешением/ключами.

Девять исходных + четыре + три follow-up диагностики аудитора приняты в активные
targets и зелёные; одна (F03) — выключена с причиной.

## В процессе / открыто

- **F03 (P1 аудитора)** — слияние двойников по слабой подписи (имя + size +
  mtime) может подменить музыку в плейлисте. Нужно решение владельца: миграция
  `track.content_hash` (рекомендация: FLAC — MD5 из STREAMINFO, прочие — хеш
  головы файла) / явное принятие риска / отказ от переезда между источниками.
  Код не менялся, тест аудитора `.disabled` до решения.
- Живые проверки, которые ни один прогон не заменяет — `docs/manual-checklist.md`
  §9: gapless против Play Next на слух, DoP на ЦАПе с receipt, Space в полях
  Settings, Stop→Play→USB, обновление Sparkle с 0.10.2, битая БД → алерт.
- Открытое из `AUDIT_RESPONSE_2026-09-12.md` §4: режимы порядка между запусками,
  порт в идентичности пароля, полный VoiceOver-маршрут, Retry/Cancel загрузки,
  живой History, измерения RSS/CPU/8 часов.
- Релиз **0.11.0** с Sparkle 2.9.6: ретест обновления; в заметках — DSD через
  PCM до подтверждения ЦАПа (D-014).

## Ключевые решения

D-014 DoP на UID · D-015 `text.muted` · D-016 Undo/подтверждение · D-017
секунды истории · D-018 `track` не удаляется мимо плейлистов. Полный ответ
аудитору по всем раундам — `AUDIT_RESPONSE_2026-09-12.md` §1–§8.

## Уроки стенда

- CLT активен → `DEVELOPER_DIR` обязателен; новые файлы App — `xcodegen generate`.
- Граф: `graphify update <path>` (AST, без LLM); `--update` ходит в ollama и
  здесь падает.
- Python-heredoc в Bash падает на `✓`/`✗` в исходнике — такие правки через Edit.
