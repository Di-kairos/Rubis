# Progress report — Session 15 (2026-09-16, Mac Mini)

Код `33e5b75` (F03 закрыт), docs `c023773`+. Пакеты **277/277** (5 пакетов),
`EscapementTests` **2/2**, Debug без warnings на Xcode 27.

## Сделано

- **F03 закрыт (D-019).** Владелец принял миграцию: `track.content_hash`
  (`v4_track_content_hash`). FLAC — MD5 несжатого PCM из STREAMINFO (нули →
  общая схема); остальные — SHA-256 трёх окон по 64 КиБ (начало, середина,
  конец) + размер. Переезд внутри источника (шаг 4), перенос между источниками
  (4b) и уборка двойников (шаг 6) сливают только при совпадении size+mtime
  **и** отпечатка; строки без отпечатка не сливаются никогда. Нетронутые файлы
  старше миграции отпечатываются на следующем скане (3b) без чтения тегов;
  файл без отпечатка — строка в scan.log. Тест аудитора
  `differentAudioMustNotReplaceAnOfflinePlaylistEntry` включён, зелёный; плюс
  обратный порядок и возвращение B, строки без отпечатка, `ContentHashTests`.
  Ответ аудитору — `AUDIT_RESPONSE_2026-09-12.md` §9.
- **Codex-ревью (ТриМозга, gpt-5.5)** — 6 замечаний, принято 2: хеш только
  головы → три окна; молчаливый `nil` → лог. Отклонено осознанно: слияние
  байт-идентичных копий в двух источниках (фича владельца, D-018), старые
  строки, переехавшие до первого пост-миграционного скана (цена, D-019),
  точность mtime в ключе уборки (доисторическое, бэклог).
- **Xcode 27 на Mac Mini** (`d8c5ca8`): `Timer.publish(...).autoconnect()`
  требует явный `import Combine` (warnings-as-errors ронял Debug);
  пост-билд переподпись выходит при `CODE_SIGNING_ALLOWED=NO` — на этой
  машине **нет Developer ID**, релиз отсюда не собрать.
- **Живые проверки §9** на Debug-сборке (`build/run-live-check.sh`, копия
  базы `…/Escapement/live-check/library.sqlite`, папка владельца
  «Collective Of Sound»): **9.7 ✅** (файл в папку → `+1` через 5 с, удалён →
  `?1`; ⌘R дважды → два полных скана подряд), **9.10 ✅** (алерт, Quit,
  Show in Finder, файл не тронут).

## В процессе

- Debug-стенд поднят, остальные пункты §9 — за владельцем (9.8, 9.5, 9.1,
  9.2, 9.12; 9.3/9.4/9.6 с ЦАПом; 9.9 с Navidrome; 9.11 после релиза).

## Осталось (переносится в kickoff 16)

1. Живые проверки §9 — см. выше. Отвечать по одному пункту, статусы в
   `docs/manual-checklist.md`.
2. **Релиз 0.11.0** — только с машины с Developer ID (MacBook):
   `RUBIS_RELEASE=1 ./Tools/make-dmg.sh`, ретест 0.10.2 → 0.11.0 (§9.11).
   В заметках: F03/отпечаток (первый скан после обновления читает по 64 КиБ×3
   на файл один раз), DSD через PCM до галки (D-014), `text.muted`, Undo
   плейлистов, honest receipt.
3. Ждём перепроверки аудитора по §8–§9.
4. Бэклог из сессии: ошибка резолва закладки при рескане (`NSCocoaErrorDomain
   259`) видна только в логе — пользователю ничего; «Add to Playlist» в
   контекстном меню трека (владелец искал его там, UX — зона владельца);
   Codex: точность mtime в ключе уборки (шаг 6 — точная, шаг 4 — секунды).

## Решения

- D-019 — тождество файла доказывает только отпечаток содержимого.
- Стенд для живых проверок на Mac Mini: Debug-сборка с `RUBIS_DB_PATH` на
  копию базы. Закладки источников app-scoped: Debug-сборка (ad-hoc) не читает
  закладки, сделанные 0.10.2, — источники добавлять заново в самом стенде.
  Восстанавливая копию базы, **не удалять WAL** (потерял добавленный
  источник, пришлось добавлять снова).

## Грабли машины (в память Claude записано)

- `git` через Xcode-shim: «not agreed to the Xcode license» →
  `DEVELOPER_DIR=/Library/Developer/CommandLineTools`.
- `swift test` нескольких пакетов параллельно с `xcodebuild` ломает резолв
  SPM-кэша; стухший `.build` от старого Xcode — `Testing.__SourceBounds`,
  лечится `rm -rf Packages/<pkg>/.build`.
- Codex CLI 0.147: модель по умолчанию `gpt-6-astra` требует новее CLI —
  звать с `-m gpt-5.5`.
